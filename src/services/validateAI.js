// import { validateAIResponse, parseAndValidateAIResponse } from "../data/schema";

// export { validateAIResponse };

// export { parseAndValidateAIResponse };

// export function formatValidationErrors(errors) {
//   if (!errors || errors.length === 0) return "";
//   return errors.map((e, i) => `${i + 1}. ${e}`).join("\n");
// }

// export function validationSummary(valid, errors = []) {
//   if (valid) return "✓ Valid";
//   const count = errors.length;
//   return `✗ Invalid — ${count} ${count === 1 ? "error" : "errors"}`;
// }
